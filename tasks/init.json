{
  "puppet_task_version": 1,
  "supports_noop": false,
  "input_method": "stdin",
  "description": "Executes a locally installed task on a machine witout a puppet agent, using a locally installed bolt.",
  "parameters": {
    "task": {
      "description": "The name of the task to run",
      "type": "String[1]"
    },
    "node": {
      "description": "The name of the node to run on",
      "type": "String[1]"
    },
    "username": {
      "description": "The username to use for connecting",
      "type": "Optional[String[1]]"
    },
    "password": {
      "description": "The password to use for connecting",
      "type": "Optional[String[1]]"
    },
    "params": {
      "description": "Parameters to pass to the task",
      "type": "Hash"
    }
  }
}
